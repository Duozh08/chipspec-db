# 2026-08-06 工作日志

## ChipSpec DB 数据合并与数量核对

### 需求背景
用户对比 Excel（芯片.xlsx 120 行移动CPU / 游戏本.xlsx 619 行）与网站数量，发现对不上；并反馈游戏本同型号多配置未合并、卡片年份重复。

### 核对结论
- **芯片**：Excel 120 颗移动 CPU **全部已在网站（0 缺失）**；网站移动端显示 126 = 120 CPU + 6 颗手动收录移动 GPU（Excel 无 GPU 列）→ 数量差异是 GPU 造成，非缺失
- **游戏本**：原按 (品牌,中文名称,型号) 分组 → 378 个，其中 33 组因名称变体（"拯救者 Y9000P"/"拯救者Y9000P 2024"、天选酷睿版/锐龙版、"AI元启"等）被错误拆分

### 修复
- `scripts/gen-data.py` 分组逻辑改为 **(品牌, 规范化名称, 型号)**：`norm_name()` = 去空格 + 去末尾年份 + 去 AI元启/酷睿版/锐龙版
- 重新生成：**378 → 345 款**（合并 33 组），0 重复 id；release 取组内最早发布时间
- **年份去重**：卡片标题 `formatCardTitle` 与详情页标题增加防御——displayName 已含年份则不再追加"-YYYY款"
- 首页 FEATURED_LAPTOPS 的 6 个 id 因规范化全部变化，已更新为新 id（如 lenovo-y9000p-16irx9、dell-alienwarem18r2-m18-r2）
- 注意：规范化后 id 不再含年份（"lenovo-y9000p-16irx9"），硬编码 id 引用需重新核对

### 技术要点
- 同型号跨多个规范名（联想 16IRX9 → 拯救者Y7000P/Y9000P/Y9000K 三组）是**不同产品共用型号**，必须按规范名分开，不能按 (品牌,型号) 一刀切合并（那样会误合并成 294 组）

### 排序 + 部署
- 新增 `useFavorites.ts` 工具：`sortByReleaseYearDesc`（按 release 年份倒序）+ `sortByYearThenFavorites`（年份倒序后收藏置顶，stable sort 保证收藏组内仍年份倒序）
- BrowsePage / LaptopListPage 改用 `sortByYearThenFavorites`
- **已部署**：commit `e91bc5a` push main（`3b3ffe0..e91bc5a`），Actions 自动部署完成，线上 JS `index-XDLO-WbA.js` 与本地构建一致（部署验证方法：node 脚本抓线上 index.html 对比 assets hash）

### 第二轮（6 项微调）
- **详情页对齐**：下方并排区改 `lg:grid-cols-2` 等宽 + 同代际/同品牌芯片卡片改为与 DieBreakdown 一致的标题栏样式（border-b + bg-slate-50 + px-4 py-2.5）
- **DieBreakdown 去长宽**：删除"长×宽"行（grid 改 3 列：面积/制程/晶体管）；SpecTable 的 Die 长×宽保持红色加粗
- **尺寸图统一**：ChipDiagram 新增 `unified` prop（详情页用），viewBox 固定为 Ryzen 9 9850HX（40×40mm，S=10）= 476×498，封装在画布内居中；对比页不用（保持真实比例对比）
- **回到顶部按钮**：新组件 `ScrollTopButton.tsx`，滚动 >400px（行业通用阈值）右下角显示，平滑回顶；已挂到 BrowsePage/LaptopListPage
- **烤机数据恢复**：`scripts/restore-stress-tests.py` 从 git v3(53dfa99) 提取 132 条 stressTest，按 (品牌归一化alienware→dell, 规范化名称/型号, 年份) 匹配到新 id → 生成 `src/data/stress-tests.ts`（92 条）。详情页从该表读取并恢复单烤CPU/GPU/双烤区块；GPU 方案卡解析型号字符串功耗（"(140W)"）显示徽标
- **条形图修复**：温度范围解析（"70-85°C" 显示原文、条宽取中间值）；晶体管/TDP/面积改用独立 display 与 barValue，避免 parseFloat 只取首数字的问题

### 第三轮（用户反馈"需求未实现"的排查修复）
- **教训**：上轮 DetailPage 加 `unified` 的 Edit 因"文件未读"被拒，读了文件后**忘了补上** → 需求3未生效。用户反馈后排查才发现。**教训：批量改文件时，Edit 失败后必须立即重试，不能跳过**
- 修复1（需求3）：DetailPage ChipDiagram 补传 `unified` → 尺寸图统一生效
- 修复2（需求1）：Die明细+同代际 grid 从 `lg:grid-cols-2` 改为与上方一致的 `lg:grid-cols-[auto_1fr]`，上下两行左右边界完全对齐
- 增强3（需求5）：烤机区块**始终显示**，无数据机型显示提示（92 款有实测数据）
- 需求2确认：SpecTable Die长宽红色加粗逻辑一直在（dieDimsMm 支持面积折算正方形，多数芯片有值）；用户可能受浏览器缓存影响
- **已部署**：commit `fa33189`（`03cef3f..fa33189`），线上 JS `index-CvL9DrnT.js` 与本地一致

### 第四轮（尺寸图裁剪修复 + 烤机数据全网搜集）
- **尺寸图大封装裁剪修复（真实bug）**：unified 模式原来只做居中偏移，50×25mm（Core Ultra 9 285H）等大封装宽 500px > 画布 476px 被裁剪。修复：unified 改为**等比缩放适配画布**（S_eff = S × min(476/vbW, 498/vbH)），所有绘制用 S_draw（含比例尺），保证物理比例一致
- **烤机数据扩充 92 → 119 条**：`restore-stress-tests.py` 匹配规则升级：年份从"相等"放宽为 **±1 年容差**（名称匹配后）+ 新增 `MANUAL_EXTRA` 手动补充字典（2026-08-06 全网搜索笔吧评测室/快科技确认：R9000P 2024、蛟龙16 Pro、泰坦GE78HX、Y9000P 2024 等）；自动匹配优先、手动补充只填缺失机型
- **SpecTable 增强**：Die 长×宽行加 `bg-red-50/70` 浅红背景 + 红色加粗文字（更醒目，避免用户看不到）
- **教训2**：临时脚本 check-deploy.cjs 曾误提交（应 git rm 后推送清理 commit d2711a5）
- **已部署**：commit `d11c3ac` + `d2711a5`（清理），线上 JS `index-CzdkVc_p.js` 与本地一致

### 第五轮（烤机数据火焰图标）
- 游戏本列表卡片：有烤机数据的机型（stressTests 表 119 款）在**标题右侧**显示橙色火焰 SVG 图标，hover 提示"该机型有第三方烤机实测数据"；flex items-start + shrink-0 保证对齐且不影响标题换行
- **已部署**：commit `3b5266f`（`d2711a5..3b5266f`），线上 JS `index-Dn5GuRC_.js` 与本地一致

### 第六轮（维修社区大升级）
- 参考迅维网等维修论坛结构改造 RepairPage：
  - 种子帖 2→7 篇（含表格内容：工具对比、BGA温度曲线），分类覆盖 6 类
  - 社区统计条（主题/回复/浏览/点赞）、公告栏（社区规范）
  - 帖子搜索（标题/内容/作者/分类）+ 排序（最新回复/最热/回复最多/点赞最多，置顶始终优先）
  - 列表增强：精华✦/已解决✓/置顶📌徽章、作者、最后回复时间、回复/浏览/点赞数
  - 详情页：帖子点赞👍、回复点赞、楼主徽章、求助帖「标记已解决」
  - 点赞防重复：localStorage `chipspec-repair-likes`
- **已部署**：commit `404c774`（`3b5266f..404c774`），线上 JS `index-snohhZim.js` 与本地一致

### 第七轮（自媒体平台 + 方案卡 Die 长宽）
- 首页三大模块上方新增「📺 刘大师兄笔记本维修」自媒体区（3 卡片）：
  - 抖音（SVG音符图标，黑底）→ https://www.douyin.com/user/MS4wLjABAAAA4D0P8CKTJtgKcLrWasR45sAGJpj7E6CIl76upgAu_STgccsSXDasACYNo_LxLDcR
  - B站（粉色TV图标）→ https://space.bilibili.com/486984223
  - 视频号（绿色播放图标）→ 预留位"敬请期待"（未提供链接）
- 游戏本详情页：CPU/GPU 方案卡增加 Die 长×宽（匹配芯片库 allChips）：normModel 精确→包含→核心编号提取（≥4位数字防误匹配），多命中取最短型号；GPU 用 RTX/GTX/RX 编号。已验证 12 个典型方案匹配正确（7945HX 不误匹配 3D 版、锐龙7 H 260 库中无则隐藏）
- **已部署**：commit `0363d2d`（`404c774..0363d2d`），线上 JS `index-CUnDQDMe.js` 与本地一致

### 第八轮（维修社区附件上传 + IndexedDB 存储）
- **存储架构决策**：localStorage 仅 5-10MB，base64 图片会撑爆 → 新增 `src/utils/fileStore.ts`：IndexedDB 封装（库 `chipspec-files` / 表 `files`），API：saveFile/getFile/deleteFile/getFileUsage/formatBytes/extractFileIds
- 发帖工具栏新增：🎬 视频（≤50MB）、📎 附件（≤20MB 多选）；**图片也改为 IndexedDB 存储**（原来 base64 嵌入 content）
- 附件以 `data-file-id` 标记插入 content（HTML 很小），详情页 useEffect 扫描 DOM 异步加载为 ObjectURL（video 内嵌播放、a 可下载、img 显示）
- 存储自我管理：单文件限制 + 总量上限 200MB、发帖页显示"已用 X / 200MB"、删除帖子自动清理关联附件（extractFileIds + deleteFile）
- 注意：这是无后端 SPA 的上限方案；若未来附件到 GB 级需接后端对象存储
- **已部署**：commit `cb2ba82`（`0363d2d..cb2ba82`），线上 JS `index-Cd8zHbT4.js` 与本地一致

### 第九轮（抖音链接更换 + 方案卡跳转）
- 抖音链接更换为新地址（HomePage 自媒体区）
- 游戏本详情页 CPU/GPU 方案卡：匹配到芯片库的整卡变 Link 跳转 `/chip/{id}`（hover 高亮阴影 + ↗ 指示 + title），未匹配保持普通卡
- **已部署**：commit `562d050`（抖音）+ `9490020`（方案卡跳转），线上 JS `index-T-YuQAsR.js` 与本地一致

### 第十轮（显卡方案卡无法跳转修复：扩充移动 GPU 库）
- **根因**：芯片库移动 GPU 仅 6 颗（Excel 只收录 CPU），80 种 GPU 方案 54 种匹配不到 → 显卡卡不变 Link
- **扩充**：gen-data.py MOBILE_GPU_TS 6→27 颗：RTX 50 系 6 颗（GB203/205/206/207，estimated）、RTX 4050、RTX 30 系 6 颗（GA103/104/107）、GTX 16 系 3 颗、RTX 20 系 2 颗、AMD Navi 22 三款
- **Ti 匹配**：matchGpu 增加 isTi 判断（3050Ti→3050Ti 而非 3050）；非 Ti 优先非 Ti
- **id 冲突**：移动 20 系/GTX16 系 id 加 `-laptop` 后缀（桌面已有同 id）
- 芯片总数 245→**266**；实测 80 种 GPU 方案 100% 匹配
- **⚠️ 事故与教训（重要）**：一次 `git add -A` 提交把 scripts/ 下 7 个脚本（gen-data.py/restore-stress-tests.py/validate-data.ts 等）意外删除（原因未明，疑似 PowerShell 清理或文件系统异常），已用 `git checkout <旧commit> -- scripts/` 恢复并补提交。**教训：git add -A 前必须 git status 检查，禁止删除 scripts 核心脚本**；提交后立即验证 npm run validate 可用
- **已部署**：commit `38927d9`+`cfd4379`（清理）+`c759944`（自媒体简化）+`063958c`（scripts 恢复），线上 JS `index-SfZ97l-J.js` 与本地一致

### 第十一轮（首页自媒体模块简化）
- 自媒体区块改为与下方三大模块一致的 3 列卡片（白底圆角+阴影，同 gap-4 同列宽）：图标缩小为纯色小图标（无背景色块）、去掉大标题和平台小标题、仅图标+「刘大师兄笔记本维修」名称；视频号预留位带"敬请期待"
- 显卡方案跳转：上一轮已修复（GPU 库 6→27 颗，80 方案 100% 匹配），用户仍反馈不跳转 → 疑为浏览器/CDN 缓存，本次部署新 hash 后 Ctrl+F5 强刷即可见
- **已部署**：commit `38927d9` + `cfd4379`（清理临时脚本），线上 JS `index-BEbCMZRB.js` 与本地一致

### 第十二轮（芯片列表卡片增强）
- ChipCard：有 Die 长×宽数据的芯片（dieDimsMm 非 null，真实长宽或面积折算）在**名称右侧显示蓝色小芯片图标**（与游戏本火焰图标布局一致，hover 提示"该芯片有 Die 长×宽数据"）
- 右下角补充规格标识徽章：制程工艺（灰）/ TDP（琥珀）/ 满载温度（红）
- **已部署**：commit `5129916`（`063958c..5129916`），线上 JS `index-2_pecnHN.js` 与本地一致

### 第十三轮（维修社区公告编辑 + 轮播）
- 公告从硬编码改为可持久化数组：localStorage `chipspec-repair-announcements`（默认 2 条种子公告，清空自动恢复默认）
- NoticeBoard 组件：多条时每 4s 淡入淡出轮播 + 「当前/总数」+ 可点击进度点；单条静态
- **已部署**：commit `7a794fb`（`5129916..7a794fb`），线上 JS `index-5rqM-L4M.js` 与本地一致

### 第十四轮（显卡方案卡 Die 长宽位置统一）
- GPU 方案卡 Die 长×宽从第二行移到**卡片右侧**（与 CPU 卡同款徽标：`rounded bg-white/70 px-1.5 py-0.5 text-[10px]`），保留功耗徽标 + ↗ 指示
- **已部署**：commit `2a800dd`（`7a794fb..2a800dd`），线上 JS `index-HJ9-pMW9.js` 与本地一致

### 第十五轮（首页模块顺序调整）
- 首页三大模块顺序：游戏本配置（第1）→ 芯片规格（第2）→ 维修社区（第3）
- **已部署**：commit `1c406d1`（`2a800dd..1c406d1`），线上 JS `index-CFQiU99f.js` 与本地一致

### 第十六轮（游戏本详情页大升级）
- 布局改两列 `lg:grid-cols-[300px_1fr]`：左列=笔记本示意图+关键参数速览（8格：平台/方案数/最大显卡功耗/刷新率/内存/硬盘/双烤功耗/年份），右列=基本规格表（12行）+处理器+显卡方案
- 新增特性标签行（自动推导）：双平台/AMD/Intel、满血/高功耗/标准功耗显卡、高刷电竞屏(≥165Hz)、高分屏(≥2.5K)、第三方烤机实测、性能释放激进(双烤≥200W)
- 基本规格表扩充：处理器平台、CPU/GPU 方案数、最大显卡功耗（正则解析 gpuOptions 的 (NNN W)）
- 新增「同品牌其他机型」推荐区块（同品牌年份倒序 6 款）
- 部署验证注意：GitHub Pages CDN 缓存 index.html 可能延迟 5-10 分钟，验证需带 no-cache 头请求
- **已部署**：commit `d512e7e`（`1c406d1..d512e7e`），线上 JS `index-Bs0rxjZo.js` 与本地一致
- NoticeEditor 组件：✎ 编辑按钮打开编辑器（多行 textarea、序号、添加/删除、保存/取消）
- **已部署**：commit `7a794fb`（`5129916..7a794fb`），线上 JS `index-5rqM-L4M.js` 与本地一致

### 第十七轮（截图识别功能）
- 新增 `/recognize` 页（导航「📷 识别」+ 首页 Hero 入口「截图识别型号」）：
  - 上传/拖拽/Ctrl+V 粘贴截图 → Tesseract.js 浏览器端 OCR（chi_sim+eng，动态 import 分包 `src-*.js`，首次下载语言包 ~10MB 有进度条）
  - 识别文本可手动修正后「重新匹配」；全部本地执行，图片不上传
- 匹配工具 `src/utils/modelMatcher.ts`：tokenize（保留中文）+ 品牌前缀剥离 + 泛词过滤（含数字且含字母/中文）；GPU 用整文本 rtx/gtx/rx 编号；laptop 用 displayName 子串匹配（阈值 3 字符）。实测 5 组场景全部正确
- 已收录 → 卡片直跳详情；未收录 → 「立即收录」→ 预填型号/类别/品牌猜测（guessBrand）→ 存 localStorage `chipspec-pending-items` 待收录清单（可移除），站长核对后补充库
- 待收录清单页内展示，说明"仅本浏览器可见"
- **已部署**：commit `8e680dc`（`d512e7e..8e680dc`），线上 JS `index-DwgmSvpA.js` + OCR chunk `src-BzvTMJLt.js` 与本地一致

### 第十八轮（OCR 下载无进度修复）
- **根因**：Tesseract.js v7 core/语言包下载阶段无进度事件（`await resp.arrayBuffer()` 一次性拉取，下载完才推进 0.5→1），UI 卡 0% 像无响应
- 修复：下载/加载阶段改**不确定进度动画**（脉冲条 + 转圈 + 文案"下载中文语言包（约12MB）…首次使用需下载，之后自动缓存"）；仅识别阶段显示真实百分比
- 新增 **150s 超时兜底**（Promise.race + clearTimeout），超时/失败明确报错 + 「重试识别」按钮
- 语言包下载后自动缓存 IndexedDB（v7 默认 writeCache，cacheMethod 未设置时走 `['write','refresh',undefined]` 分支），二次识别免下载
- **已部署**：commit `b5d845c`（`8e680dc..b5d845c`），线上 JS `index-CrTiG-Bv.js` + OCR chunk `src-BH5f4t7K.js` 一致

### 第十九轮（识别改弹框 + 提交即收录 + 徽标）
- 识别从独立页改为**全局弹框**（`src/components/RecognizeModal.tsx` + `src/context/RecognizeContext.tsx`，Provider 挂 main.tsx）：首页 Hero 按钮/导航「📷 识别」点击弹出；弹框内粘贴(Ctrl+V)/上传 → 「立即识别」→ 结果分区
- 弹框 UI 美化：统计条（匹配芯片/游戏本/待收录 3 格）+ 识别文本可编辑（重新匹配）+ 芯片(蓝)/游戏本(紫)结果卡片 + 未收录候选(琥珀) + 收录表单内联 + 底部已收录清单胶囊；Esc/遮罩关闭，结果点击跳详情（先关弹框）
- 提交收录**即时生效**（去审核流程）：入 localStorage 已收录清单，并自动 `window.open(cn.bing.com/search?q=型号+规格)` 全网搜索供核对
- **右上角待收录徽标**：App header 红色胶囊「待收录 N」+ 呼吸动画圆点，点击打开弹框；经 `chipspec-pending-updated` 自定义事件 + storage 事件同步（pendingStore notifyPendingChanged）
- 删除旧 RecognizePage.tsx 与 /recognize 路由
- **已部署**：commit `5114003`（`b5d845c..5114003`），线上 JS `index-DZ-0fdzK.js` + OCR chunk `src-BBHuH1PO.js` 一致

### 第二十轮（去掉导航对比入口）
- 移除顶部导航栏「对比」链接（右上角）；对比功能本身（/compare 路由、CompareTray、ChipCard +对比按钮）保留
- **已部署**：commit `b4fb9a8`（`5114003..b4fb9a8`），线上 JS `index-BMM4OyOr.js` 一致

### 第二十一轮（识别优化 + 首页小知识升级）
- 识别：未收录候选带**类型徽章**（芯片蓝/游戏本紫，guessCandidateType 关键词判定，游戏本候选修复）；待收录**去掉内联表单**，点「立即收录」直接收录（自动类别/品牌）+ 自动全网搜索 + 显示"✓ 已收录"状态
- 首页 logo `items-baseline`→`items-center`（中英文水平对齐）；识别弹框 header 摄像机图标块、导航"📷 识别"emoji 均去除
- 芯片小知识 3→9 篇：新增 Intel Core(i9-14900K)/AMD Ryzen(9950X3D)/NVIDIA RTX(4090) 命名规则逐段解读、移动端后缀速查(HX/H/HS/U/P)、制程工艺、满血功耗；KnowledgeModal 弹框（卡片点击弹出详情段落）
- 知识模块改横向滚动（snap-x + w-72 卡片）+ 左右箭头 scrollBy(±320)
- **已部署**：commit `f0fed89`（`b4fb9a8..f0fed89`），线上 JS `index-uPj7Dnbs.js` + OCR chunk `src-Dm8ZqI60.js` 一致

### 第二十二轮（收录改 AI 自动补全管道）
- 点击「立即收录」不再跳转必应手动搜索 → 进入 **AI 自动补全队列**（本地已收录清单，状态显示"AI 自动补全中…"）
- 新增「⬇ 导出待补全清单」：下载 `chipspec-pending-requests.json`（exportPendingItems，含名称/类别/品牌/备注）
- 新增 `scripts/fill-from-pending.py`：解析清单 → 生成符合网站 Chip/Laptop 结构的数据骨架 `scripts/pending-templates.ts`（规格 null）→ AI 逐条 WebSearch 补全 → 合并入库 → validate → 部署
- 数据诚信：骨架不确定字段 null，填充须有公开来源
- **已部署**：commit `dc73d83`（`f0fed89..dc73d83`），线上 JS `index-DnEgwYX3.js` + OCR chunk `src-DcObSaFv.js` 一致

### 第二十三轮（收录全自动闭环）
- 点击「立即收录」**全自动**：`src/utils/localCatalog.ts` 生成本地条目（名称/类别/品牌/状态/时间戳）+ best-effort 维基百科 REST API 自动获取简介（AbortController 6s 超时，失败不阻塞）+ 同步右上角待收录徽章
- 新增 `src/components/NewCatalogSection.tsx`：芯片/游戏本**列表页顶部『新收录』区块**，红色 NEW 徽章（isNewItem：createdAt 24h 内），每分钟刷新检查，满 24h 自动消失；可手动移除
- 本地条目状态：pending（AI 补全中）/ filled（已补全）；深度规格补全（Die/TDP）仍走导出清单管道
- **已部署**：commit `264cce3`（`dc73d83..264cce3`），线上 JS `index-B5THBcqx.js` + OCR chunk `src-D2eTZmYV.js` 一致
