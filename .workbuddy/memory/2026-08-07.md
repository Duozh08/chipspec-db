# 2026-08-07 工作日志

## CloudBase 后端部署完成（收录全自动闭环上线）

### 环境更换
- 新环境：`duozhu08-tengfei-d1eqlp0bae59452`（个人版，ap-shanghai，到期 2026-09-07），替代原 `tengfei-duozhu08-d6eo3ar7df7e8cc`（体验版）
- 前端 `src/utils/apiClient.ts` 的 `CLOUDBASE_ENV_ID` 已更新为新环境 ID

### 云函数部署（全部 Event 型 + 网关路由，非 HTTP 型！）
- `collect`：写库 + 异步触发 autoFill；网关路由 `/collect`
- `autoFill`：DeepSeek 补全（环境变量 `DEEPSEEK_API_KEY` 已配置）；超时 300s
- `list`：查询收录结果；网关路由 `/list`
- 函数权限已放开匿名访问（CUSTOM securityRule `{"invoke": true}`）
- catalog 集合权限：ADMINONLY
- 访问域名：`https://duozhu08-tengfei-d1eqlp0bae59452.service.tcloudbase.com/{collect|list}`（已测试 HTTP 200）

### 关键坑（必须记住）
1. **Event 云函数 + HTTP 网关调用时**：请求体包在 `event.body`（JSON 字符串）里，不是平铺参数！已加 `parseParams()` 兼容（collect/list）
2. **CloudBase 嵌套 update 报错**：原字段为 `null` 时 `update({spec: {...}})` 会报 `Cannot create field 'brand' in element {spec: null}` → 改用 `doc(id).set({...})` 全量替换（需先 get 保留 createdAt）
3. **MCP createFunction 传 `type=HTTP` 会失败**（要求 scf_bootstrap）→ 必须 Event 型 + `manageGateway createRoute`（upstreamResourceType=SCF）
4. autoFill 全量 set 时要定义所有变量（曾因漏 `const brand` 报 ReferenceError）
5. 云函数代码更新后 invoke 可能短暂命中旧实例，等几秒重试

### 验证结果（端到端全通）
- RTX 5060 Laptop（chip）→ autoFill 补全 filled ✅
- 拯救者Y7000P 2025（laptop）→ filled ✅
- ROG 幻16 Air 2025（laptop）→ collect 自动触发 autoFill → 60s 内 filled ✅（自动链路验证通过）

### 其他
- `cloudfunctions/README.md` 已更新为实际部署状态
- `npm run build` 通过；`dist/` 已重新生成
- 测试数据 3 条仍在 catalog 集合（可用于演示/验收）

## GitHub 部署（23:23）
- 提交 `3b95ad7`（feat: CloudBase 后端部署上线，收录全自动闭环）已 push 到 `origin/main`
- 9 个文件变更：apiClient.ts（新环境 ID）、三个云函数 index.js、cloudfunctions/README.md、dist/autoFill.zip、记忆文件
- GitHub Actions（.github/workflows/deploy.yml）自动触发构建部署到 GitHub Pages
- 删除遗留临时文件 cloudfunctions/dist/autoFill.zip.b64（已被 gitignore，不会入库）

## 截图识别匹配修复（23:33，提交 f72b5f9）
用户反馈：截图 ROG 幻16 Air 2025 识别不出，站内有 4 款幻16（22×2/23/24）但无 2025 款，也没有待收录选项。
根因（modelMatcher.ts 三处 bug）：
1. matchLaptopsInText 单向匹配（站内名.includes(token)），OCR 输出完整串 "rog幻16air2025" 时 token 比站内名长 → 匹配失败 → 改双向 includes
2. tokenize 后 "幻16" 3 字符被 length>=4 过滤 → 候选为空 → 下限降为 3
3. 无年份感知 → 新增：文本含年份且站内同系列无该年份款时，生成「款名+年份」待收录候选（hint 提示"站内已收录 X 款"）
另外：排除纯数字 token（2025/140w）避免误匹配所有同年款；系列 key 归一化去 displayName 末尾年份（拯救者Y7000P2025→拯救者y7000p）避免重复候选
验证：ROG 幻16 Air 2025 → 待收录 [ROG幻16 2025, ROG幻16Air 2025] ✅；ROG 幻16 2023 → 站内已有不误报 ✅；RTX 5060 Laptop → 不误匹配游戏本 ✅
UI：RecognizeModal 待收录列表支持 hint 文案显示

## 收录补全状态轮询（2026-08-08 08:16，提交 32898bf）
用户反馈：昨晚点击收录"还在补全中"。实际后端 2 秒就 filled（ROG 幻16 Air：23:18:57→23:18:59），但前端从不查后端状态，UI 永远显示补全中。
修复：
1. 新增 src/hooks/useCollectSync.ts：每 5s 轮询后端 list('filled')，把已补全同步到 pendingStore（markPendingFilled）与 localCatalog（saveLocalCatalogStatusByName）
2. apiClient 新增 apiList() 与 CatalogRecord 接口
3. pendingStore PendingItem 增加 status/filledAt；addPendingItem 默认 status:'pending'
4. RecognizeModal 底部清单：补全中 spinner / ✓ 已补全（绿）+ 时间
5. App.tsx 右上角：新增绿色「✓ 已补全 N」徽章，红色「待收录」只显示未补全数
**关键坑**：安全域名（CORS）里没有 duozh08.github.io！Node fetch 无 CORS 检查能通，但浏览器跨域会被拦。已用 envDomainManagement 添加 duozhu08.github.io + :443（ENABLE 生效，ACAO 正确返回）。以后改环境/域名必须记得配安全域名。

## 重新匹配按钮修复（2026-08-08 08:25，提交 5f5d35e）
用户反馈：识别弹框「重新匹配」用不了。
根因：handleRematch（useCallback）定义在 runMatch 之前，依赖数组 [runMatch, text] 在定义时求值触发 TDZ 错误（Cannot access 'runMatch' before initialization），组件渲染异常。
修复：
1. handleRematch 移到 runMatch 定义之后
2. 按钮点击用 textAreaRef.current.value 实时读取（避免闭包捕获旧 text）
3. 增加点击反馈（绿色提示条显示重新匹配的文本）
4. 按钮 type=button + hover/active 样式
验证：tsc 通过；dev server HMR 正常；修改文本后重新匹配结果确实更新（ROG幻16 2024→2025）
⚠️ 教训：React 组件内 useCallback/函数引用前向引用会 TDZ 报错，定义顺序必须保证依赖在前。

## 独立待收录明细弹框（2026-08-08 09:06，提交 a9965ce）
用户反馈：右上角待收录点开是识别弹框，多此一举，只需显示收录明细与状态。
实现：
1. 新增 src/components/PendingListModal.tsx：统计条（总收录/补全中/已补全）+ 明细列表（类型徽章、收录时间、补全时间、✓已补全/AI补全中 spinner、移除按钮）+ 导出按钮；每 5s 轮询刷新（useCollectSync）
2. RecognizeContext 新增 openPending()，两个弹框独立管理
3. App.tsx 右上角「待收录」「已补全」徽章 → openPending（明细弹框）；导航「识别」→ openRecognize（识别弹框）不变

## 游戏本提交缺失芯片方案（2026-08-08 09:53，提交 9e1cae0）
用户需求：部分游戏本 GPU/CPU 方案不全，全网搜索有遗漏 → 详情页增加手动提交方案并收录。
实现：
1. 新增 src/utils/laptopChipAdditions.ts：本地存储 `chipspec-laptop-chip-additions`（Record<laptopId, {cpus,gpus}>）
2. LaptopDetailPage 新增「补充缺失的处理器/显卡方案」表单：CPU/GPU 切换 + 输入 + 提交并收录
3. 提交逻辑：**优先匹配移动端芯片库，未命中回退桌面端**（matchCpu/matchGpu 增加 formFactor 参数）
   - 站内已有 → addLaptopChipAddition 直接加入本机方案（可跳转芯片详情，标「补充」徽章）
   - 站内没有 → apiCollect（后端 AI 补录）+ addPendingItem（右上角清单）+ addLocalCatalogItem（芯片列表 NEW）+ 加入本机方案
4. 方案卡合并渲染（静态 + 补充，去重），Intel/AMD/GPU 卡片均加「补充」标识
5. 跨页 storage 同步（chipspec-laptop-chip-additions 事件 + storage 事件）
注意：allChips 桌面库排在移动库前，提交 GPU 如 RTX 4080 Laptop 时直接全库匹配会误命中桌面 RTX 4080 Super → 必须移动端优先。
补充数据仅存本机，站长核对后可并入正式库。
