# 2026-08-05 工作日志

## ChipSpec DB v4 大版本改造（10 项需求一次性完成）

### 1. Excel 数据同步（scripts/gen-data.py）
- 编写 `scripts/gen-data.py`：读取 `D:/Desktop/芯片.xlsx`（120 颗移动端 CPU）+ `游戏本.xlsx`（619 行 / 378 个型号）自动生成 TS 数据
- 移动端芯片 126 颗 = Excel 120 CPU + 从 git 恢复的 6 颗手动收录移动 GPU（RTX 4090/4080/4070/4060/3060 Laptop、RX 7600M XT）—— 避免覆盖丢失
- 游戏本从 132 款 → 378 款（16 品牌：新增戴尔/神舟/小米/荣耀/技嘉/华为/机械师/雷神）
- 全库芯片 245 颗，validate 0 错误

### 2. 第二轮修复（5 项微调）
- **上传图片修复**：根因是上传按钮在 `<Link>` 内点击冒泡触发跳转；改为右下角 ✎ 圆形小按钮，onClick 里 `preventDefault + stopPropagation`；未上传显示原 SVG，上传后右下角多一个删除按钮
- **gen-data.py 加固**：GPU 数据与 `getLaptopById` 固化进脚本（重跑不会丢）；id 去重（`unique_id` 冲突追加 -2/-3），修复 7 个重复 id（"天选6 酷睿版/锐龙版"中文被 slugify 吞掉导致同 id）
- **详情页右侧补空**：右侧改 `flex flex-col` + 底部「规格解读」卡片（TDP/Die面积/晶体管/温度条形图 + 标签），flex-1 撑满与左侧底部对齐
- **首页热门推荐**：恢复「热门速览」= 热门芯片 8 颗（桌面4+移动4，含 nvidia-geforce-rtx-4090 等）+ 热门游戏本 6 款
- **关注星标**：芯片/游戏本列表卡片右上角黄色五角星 ★（text-yellow-400 drop-shadow），原红色徽标移除；游戏本卡片 CPU 徽标行加 pr-5 避让
- Die 长×宽 加粗红色在 SpecTable + DieBreakdown 双处生效

### 3. 第三轮（4 项微调）
- **首页内容扩充**：新增「数据概览」统计面板（芯片/桌面/移动/CPU/GPU/代际/游戏本数 + 品牌分布色条）+「芯片小知识」行业科普卡（Die/封装/TDP）
- **芯片卡片改左图右文**：类似游戏本卡片布局，左侧芯片图（ChipPhoto h-24）+ 右侧信息区，上传 ✎ 按钮在图片右下角
- **详情页布局对齐**：左侧改 flex-col（图+图例+速览），同代际芯片从左侧移出，与 DieBreakdown 并排 `lg:grid-cols-[1.5fr_1fr]`；无同代际时兜底显示同品牌芯片
- **游戏本卡片显示全部方案**：GPU/CPU 方案区列出全部型号，带色块圆点（NVIDIA 绿 / AMD 红 / Intel Arc 蓝 / CPU 平台色）

### 2. 数据结构重构（breaking change）
- `Laptop` 接口：`cpu/gpu` → `cpuOptions[]/gpuOptions[]`；删除 `priceCny`；新增 `cpuPlatform()` 判定 Intel/AMD
- 旧品牌 `alienware` 并入 `dell`；`LAPTOP_BRAND_LABELS` 扩展 16 品牌
- 新增 `src/hooks/useFavorites.ts`：localStorage `chipspec-favorites`，收藏排在列表前

### 3. 页面改造
- 主页三大模块：芯片/游戏本/维修（去除桌面/移动端拆分）
- 芯片列表：FilterBar 已有形态筛选（桌面/移动）；ChipPhoto 支持本地上传图（JPG/PNG/WebP ≤2MB，localStorage `chipspec-chip-photos`）
- 芯片详情页：图下方新增「关键参数速览」+「同代际芯片」卡片补空；Die 长×宽行加粗红色（SpecTable）
- 游戏本列表：两列卡片（左上名称/右上CPU平台/左下笔记本SVG/右下GPU方案）
- 游戏本详情：CPU 按 Intel/AMD 分组、GPU 方案各占一行每行 2 个带序号徽标；去掉价格
- 新增 `src/pages/RepairPage.tsx` 维修论坛：发帖/详情/回复/删帖，contentEditable 富文本工具栏（加粗/斜体/字号/字体/颜色/列表/对齐/链接/图片上传），localStorage `chipspec-repair-posts` 存储

### 4. 第四轮（4 项微调）
- **芯片列表改两列**：BrowsePage 网格 `sm:grid-cols-2`（与游戏本一致）
- **上传跳转彻底修复**：`ChipPhoto` 拆为纯显示组件（支持 `photo` prop）+ 新导出 `ChipPhotoUpload` 独立控制组件；上传/删除按钮作为 **Link 兄弟节点**绝对定位在图片右下角（z-20），彻底杜绝点击触发导航
- **维修论坛表格**：工具栏新增「⊞ 表格」按钮（prompt 输入行x列，首行自动为表头）；contentEditable `onPaste` 检测剪贴板 HTML 含 `<table` 时用 insertHTML 保留格式粘贴（Excel/网页表格可直接复制）；编辑区与详情渲染均加 table 边框样式（`[&_table]:border-collapse` 等）
- **帖子置顶**：Post 加 `pinned?: boolean`；详情页右上角「📌 置顶/取消置顶」；列表排序 pinned 优先再按时间倒序，置顶帖显示「📌 置顶」标签

### 4. 已知变化
- 新数据来自 Excel，stressTest 烤机数据不再包含（Excel 无此列），详情页烤机区块自动隐藏
- 待办：后续可跑 `git push` 自动部署 GitHub Pages

### 技术要点
- PowerShell 5.1 无 `String.Replace(s,s,StringComparison)` 重载；sed 处理中文反引号易留字面 `$` → 用 .NET File.ReadAllText/WriteAllText 稳妥
